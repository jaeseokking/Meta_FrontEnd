import React from 'react';
import styled from "styled-components";

const MobileLayout = (props) => {
    return (
        <Wrapper>
            모바일 레이아웃
        </Wrapper>
    );
}

export default MobileLayout;


const Wrapper = styled.div`
    backgroundColor : rgba(1, 78, 136, 0.9);
    width : '100%';
`
